package com.src.getabstract;

interface StandardInterfac {
	
	void companyName(String validName);
	void numOfEmployee(int validNumber);
	final String nameVal = "Agree";
   // interface has only static and final variables.
}
